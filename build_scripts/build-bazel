#!/usr/bin/env sh
set -o errexit -o nounset

docker exec --tty --workdir "$(pwd)" \
    bazel-server bazel build //jb:extract_build_book

mkdir -p public
cp -r "bazel-bin/jb/extract_build_book/tmp/jb/_build/html" public/executable-notes
